import { OpaqueToken } from '@angular/core';

export const CategoryListToken = new OpaqueToken('CategoryListToken'); 

export const CategoryList = {
    categories: ['Bevarages', 'Condiments', 'Produce', 'SeaFood', 'Misc']
};
